#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

int main()
{
    
int x = 10;
int y = 2;
int z = 0;

//z = x + y; // adição simples
//z = x - y; //subtração simples
//z = x * y; // multiplicação simples
//z = x / y; // por ser integral, não armazena decimais, teria que ser float.
//z = x % y; // indica a sobra, módulo
//x++; //incrementa em 1
//x--; //subtrai o valor em 1

//x = x + 2 ;
//x+=2 ; // os dois são a mesma coisa, da pra fazer isso com certos operadores (+, *, /, %.)


printf("%d", x) ;



    return 0;
}
